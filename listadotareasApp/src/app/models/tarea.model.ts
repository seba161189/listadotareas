export interface Tarea {
    id: number; // Identificador único de la tarea
    descripcion: string; // Texto descriptivo de la tarea
    completada: boolean; // Indica si la tarea está completada
  }